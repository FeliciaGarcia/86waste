angular
  .module('project5')
  .component('businesses', {
    templateUrl: '<%= asset_path("components/businesses/businesses") %>',
    controller: BusinessesController
  });

BusinessesController.$inject = [];

function BusinessesController() {
  var vm = this;

  vm.message = 'The business page is working';
}